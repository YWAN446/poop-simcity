# Raw simulation data (large parquet / 736MB CSV) — provided, not versioned here
/dataset_00/
/dataset_sdc-10k/

# Source preprint and scratch extraction
*.pdf
/paper_text.txt
/inspect_data.py
/inspect2.py

# The generated data bundle IS committed (so the app runs from a fresh clone
# without needing Python or the raw simulation data). Regenerate it with the
# preprocessor if needed — see the README.

# Exception: the dataset_sdc-10k bundle (89,350,562 bytes across 23 files) is NOT
# committed. That's a lot of binary history to add irreversibly, so whether to commit it is left to the
# repo owner rather than done automatically. Regenerate it locally with the
# preprocessor command documented in the README; dataset_00's bundle above
# remains committed as before.
/app/public/data/dataset_sdc-10k/

# Node / web app
node_modules/
app/node_modules/
dist/
app/dist/
.vite/
*.log

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
